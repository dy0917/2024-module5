const {addNumber} =require("./calculatorController")

const bFunction = (req, res) => {
  console.log();
};

const aFunction = (req, res) => {
    return false;
  };
module.exports = { 
  bFunction,
}
